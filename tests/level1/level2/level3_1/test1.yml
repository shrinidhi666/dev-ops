test1:
  cmd.run:
    command:
      - ls "/tmp" | gawk '{print "wtf directories - "$1}'
      - pwd
      - dmesg|tail
      - echo "{{ slaveconst['punk'] }}"
    user: shrinidhi
    path: /home/shrinidhi
    shell: true

{% if slaveconst['wtf'] == 'wtf1' %}
wtf:
  cmd.run:
    command:
      - echo "shit" | tee -a /tmp/shit
    user: shrinidhi
    path: /home/shrinidhi
    shell: true
{% endif %}

test1:
  cmd.run:
    command:
      - ls /home/

    user: shrinidhi
    path: /home/shrinidhi
    shell: true
