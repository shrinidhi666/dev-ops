{% if slaveconst['wtf'] == 'wtf1' %}
- level3_8:
    cmd.run:
      command:
        - echo "test2"
        - echo {{ slaveconst['wtf'] }}
      user: shrinidhi
      shell: true
{% endif %}

- level3_8_def:
    cmd.run:
      command:
        - echoo "test2 def"
      user: shrinidhi
      shell: true

