- test1:
    cmd.run:
      command:
        - cd /opt/dev-ops/; git pull ; cd - ; echo "testing"
        - supervisorctl restart devops-slave
      user: shrinidhi
      path: /home/shrinidhi
      shell: true

{% if slaveconst['wtf'] == 'wtf1' %}
- wtf:
    cmd.run:
      command:
        - echo "shit" | tee -a /tmp/shit
      user: shrinidhi
      path: /home/shrinidhi
      shell: true
{% endif %}

- test4:
    file.sync:
      source: devops://jinjafile.jinja
      dest: /tmp/jinja_testing_from_server.jinja
      user: shrinidhi
      group: artist
      mode: 777
